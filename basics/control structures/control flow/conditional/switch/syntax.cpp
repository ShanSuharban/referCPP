#include<iostream>
using namespace std;
void main () {
/*
    switch (exp) {
        case value1: action1;
                     break;
        case value2: action2;
                     break;
        case valuen: actionn;
                     break;
        default: action;
    }

    switch can only input the integer number and the character, not floating point and other types. But if can input all types of numbers and the variables.
    
 -----------------------------------------------------------------------------
 
 switch (1,2) {
 case 1: value 1;break;
 case 2: value2;break;
 default: value;
 }
 
 In this condition switch is switch its value to the 2. So the case 2 is executed.
 
*/
}
