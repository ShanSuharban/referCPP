#include<iostream>
using namespace std;
int main (){
    /*
syntax:     if (exp) {

            }

            else if (exp) {

            }

            else if (exp) {

            }

            else {

            }

if the boolean expression is true, then the statements inside the if block will execute otherwise it checks the else if part and again check the exp in the else if is true or not.if true then the statements within the else if part is executed .This cycle gets repeated and if none matches the expression is true, then it goes to the else part and the statements inside the else part is executed.
    
    */

    return 0;
}