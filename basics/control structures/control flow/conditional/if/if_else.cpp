#include<iostream>
using namespace std;
int main(void) {
   /* 
   syntax:

    if (condition)
        statement;
    else
        statement;

    "if more than one statement is needed, the statements should be wriiten inside the curly braces of if and else."

   syntax:

    if (condition) {

    }
    else {

    }

    " Dont put ; after if (condition)"
    " Dont put relational exp in paranthesis after else keyword like : "else (a<b)"
      because no relational expression should follow else keyword"
      
   */
return 0;
}